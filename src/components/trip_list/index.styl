@require '../../vars'

.z-trip-list
  > .placeholder
    text-align: center

    > .icon
      width: 145px
      height: 124px
      background-image: url($cdnUrl + '/empty_state/trip_following_empty.svg')
      background-size: 100%
      margin: 48px auto 0 auto

    > .title
      font-title()
      margin-top: 16px
      color: getColor('$primary500')

    > .description
      font-body2()
      margin-top: 8px
