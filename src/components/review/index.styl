@require '../../vars'

.z-review
  display: flex
  width: 100%
  box-sizing: border-box
  overflow: hidden
  position: relative

  > .avatar
    margin-right: 16px
    cursor: pointer
    position: relative

  > .content
    flex1()
    min-width: 0 // fix

    > .title
      font-title()
      margin-bottom: 8px

    > .body
      font-body2(no)
      // font-size: 14px
      position: relative
      z-index: 2
      padding-right: 16px
      // margin-top: 6px
      overflow: hidden

    > .attachments
      margin: 8px 0

  @media (min-width: $breakPointMedium)
    > .content
      > .info > .name
        max-width: none
