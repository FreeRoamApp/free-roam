@require '../../vars'

.z-places-list
  flex1()
  overflow: auto
  -webkit-overflow-scrolling: touch
  padding: 8px

  > .g-grid

    > .place
      &.has-shadow
        shadow-1()
        background: getColor('$tertiary0')
        border-radius: 2px
        padding: 8px 16px
        min-width: 0
        margin-bottom: 12px
