@require '../../vars'

.p-edit-trip-add-stop
  flex1()
  display: flex
  flex-direction: column

  &.has-trip-route
    padding-bottom: 42px

  @media (min-width: $breakPointMedium)
    &.has-trip-route
      padding-bottom: 0
